//import produce from 'immer';
///import { SET_ALBUM_ID, CLEAR_ALBUM_PAGE, SET_LOADING } from './constants';

// The initial state of the App
export const initialState = {};

/* eslint-disable default-case, no-param-reassign */
const contactDetailsFormReducer = (state = initialState, action: any) => {};
// produce(state, draft => {
//   switch (action.type) {
//     case SET_ALBUM_ID:
//       draft.albumId = action.payload;
//       break;

//     case CLEAR_ALBUM_PAGE:
//       draft.albumId = undefined;
//       break;

//     case SET_LOADING:
//       draft.loading = action.payload;
//       break;
//   }
// });

export default contactDetailsFormReducer;
